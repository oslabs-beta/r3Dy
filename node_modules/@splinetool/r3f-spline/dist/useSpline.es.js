import SplineLoader from "@splinetool/loader";
import { useLoader, useGraph } from "@react-three/fiber";
function useSpline(url) {
  const scene = useLoader(SplineLoader, url);
  return useGraph(scene);
}
export { useSpline as default };
